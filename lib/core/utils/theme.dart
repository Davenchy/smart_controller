import 'package:flutter/material.dart';

const Color primaryColor = Color(0xFF1C2834);

final defaultTheme = ThemeData(
  textTheme: TextTheme(),
  primarySwatch: Colors.blue,
  primaryColor: primaryColor,
);
